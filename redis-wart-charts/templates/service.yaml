apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-wart-charts.fullname" . }}
  labels:
    {{- include "redis-wart-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-wart-charts.selectorLabels" . | nindent 4 }}
