apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis-wart-charts.fullname" . }}-test-connection"
  labels:
{{ include "redis-wart-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "redis-wart-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
